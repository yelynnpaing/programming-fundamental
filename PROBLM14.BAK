#include<iostream.h>
#include<conio.h>
void main()
{
	clrscr();
	int temp_count = 0;
	float c_temp, f_temp;
	while(temp_count < 15)
	{
		cout << "\nEnter f_temp";
		cin >> f_temp;
		c_temp = (f_temp - 32) * 5.0/9.0;
		cout << "c_temp : " << c_temp;
		temp_count++;
	}
	getch();

}